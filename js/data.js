// Combines the individual data files (js/data/about.js, social-links.js,
// news.js, publications.js) into the single siteData object the page uses.
// Edit content in those files, not here.
const siteData = {
  about: aboutData,
  socialLinks: socialLinksData,
  news: newsData,
  publications: publicationsData
};
